function Setup() {
  return (
    <div
      style={{
        padding: '24px',
        color: '#fff',
      }}
    >
      <h1>Setup</h1>

      <p>
        Create agents and define their prompts, models, parameters, tools,
        permissions, resources, and available configuration options.
      </p>
    </div>
  )
}

export default Setup