function Analytics() {
  return (
    <div
      style={{
        padding: "24px",
        color: "#fff",
      }}
    >
      <h1>Analytics</h1>

      <p>
        Review agent performance, execution telemetry, model and tool usage,
        budgets, costs, task completion, errors, workload, and other project
        analytics.
      </p>
    </div>
  );
}

export default Analytics;