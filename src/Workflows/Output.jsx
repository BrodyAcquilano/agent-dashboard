function Output({runtime}) {
  return (
    <div
      style={{
        padding: '24px',
        color: '#fff',
      }}
    >
      <h1>Output</h1>

      <p>
        View agent runs, reports, generated files, research, messages, and
        other project outputs.
      </p>
    </div>
  )
}

export default Output